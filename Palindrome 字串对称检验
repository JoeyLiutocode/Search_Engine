def is_palindrome(s):
    if len(s) == 1 or s == '': ##if string length is one, then string must be palindrome
        return True
    else:
        if s[0] == s[-1]:
            return is_palindrome(s[1:-1])
        else:
            return False
